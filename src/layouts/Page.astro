---
import Main from "../layouts/Main.astro";

const { frontmatter } = Astro.props;
---

<Main title={frontmatter.slug}>
  <div
    class="mb-5 w-5/6 md:w-4/6 lg:w-3/6 flex flex-col justify-center mx-auto"
  >
    <!-- <h1
      class="text-2xl font-bold mb-1 font-josefin text-gray-800 dark:text-gray-400"
    >
      {frontmatter.title}
    </h1>
    <h3 class="text-xl mb-1 font-josefin text-gray-800 dark:text-gray-400">
      written by <span class="font-bold">{frontmatter.author}</span>
    </h3>
    <h3 class="text-xl mb-5 font-josefin text-gray-800 dark:text-gray-400">
      published on <span class="font-bold">{frontmatter.date}</span>
    </h3> -->
    <!-- <div class="text-lg mb-5 text-gray-900 dark:text-gray-300 font-serif"> -->
    <div class="prose dark:prose-invert prose-img:rounded-xl">
      <slot />
    </div>
  </div>
</Main>
